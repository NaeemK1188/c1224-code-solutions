console.log('hello, Node!');
console.log(process.env); // .env has all the environment variables used
// by the server and we can name it in name we want .env.name
// has many information like shell:, ...
// these variables will change when we use different device

// const e:object = process.env;
// console.log('Path', e.PATH);
